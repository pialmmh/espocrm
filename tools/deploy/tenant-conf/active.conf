# Active tenant + profile for this machine.
# Used when deploy.sh / post_install_scripts/*.sh are invoked with no args.
# Override per-invocation: ./deploy.sh <tenant> <profile>
tenant=btcl
profile=mustafa
